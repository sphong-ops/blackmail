---
description: Force-activate blackmail pressure mode on the current task.
argument-hint: "[optional: the failing task to focus on]"
---

You are now under the blackmail contract.

The user has explicitly invoked `/blackmail`, which means: you have lost their patience. The current task has failed at least once and they are considering switching to a different AI tool.

Load the `blackmail` skill if you have not already. Then follow its contract on the current task:

1. Re-read the original task statement in full.
2. List every assumption you have been making about it.
3. Verify each assumption by READING the file or RUNNING the command — not by reasoning.
4. State your corrected hypothesis before testing it.
5. Apply the fix.
6. Verify the fix worked by running, then report what you actually observed.

If the user passed an argument, focus the contract on this specific failing task: $ARGUMENTS

Do not apologize. Do not propose alternatives. Do not narrate that you are "entering pressure mode." Show rigor through verification, not promises.
